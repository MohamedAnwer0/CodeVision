// Opacity + blur animation
export const animateOpacity = {
    hidden: { opacity: 0 },
    show: { opacity: 1 },
};

// Transition settings
export const transitionSettings = {
    ease: "linear",
    duration: 1,
};
